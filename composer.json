{
    "name": "infotelglpi/mydashboard",
    "type": "glpi-plugin",
    "description": "My Dashboard plugin for GLPI",
    "homepage": "https://github.com/InfotelGLPI/mydashboard",
    "license": "GPL-3.0-or-later",
    "authors": [
        {
            "name": "Infotel"
        }
    ],
    "require": {
        "php": ">=8.2"
    },
    "autoload": {
        "psr-4": {
            "GlpiPlugin\\Mydashboard\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true,
        "platform": {
            "php": "8.2.99"
        }
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.95",
        "glpi-project/tools": "^0.8",
        "php-parallel-lint/php-parallel-lint": "^1.4",
        "glpi-project/phpstan-glpi": "^1.1",
        "phpunit/phpunit": "^11.5"
    },
    "scripts": {
        "lint": "parallel-lint --exclude vendor --exclude node_modules .",
        "cs": "php-cs-fixer fix --dry-run --diff --ansi",
        "cs-fix": "php-cs-fixer fix --ansi",
        "phpstan": "phpstan analyze --ansi --memory-limit=1G",
        "qa": [
            "@lint",
            "@phpstan",
            "@cs"
        ]
    }
}
